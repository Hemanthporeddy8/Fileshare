{
  "name": "fileshare",
  "version": "1.0.0",
  "description": "P2P file sharing — no login, files never stored permanently",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "qrcode": "^1.5.4",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
